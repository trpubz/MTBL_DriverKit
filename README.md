# DriverKit

## Installation
This package was packaged with poetry, to install with `pip install` ensure that `DriverKit @ git+https://github.com/trpubz/SeleniumDriverKit.git` syntax is located in requirements.txt

## Dependencies
`selenium='4.10'` is required
